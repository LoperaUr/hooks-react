import React from "react";
import Header from "../helpers/Header";

const Albums = () => {
  return (
    <section>
      <Header />
      <main>Albums</main>
    </section>
  );
};

export default Albums;
